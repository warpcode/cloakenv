cache:
  default_ttl: "10m"

keyring:
  prefix: "cloakenv_test"

providers:
  invalid_repo:
    provider: "yaml"
    database_path: "./testdata/test_hosts.yaml"
    entries_key: "non_existent_key"
  valid_repo:
    provider: "yaml"
    database_path: "./testdata/test_hosts.yaml"
    entries_key: "hosts"
